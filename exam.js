function exam(input){

    let badGrades = Number(input[0]);
    let index = 1;
    let currentInput = input[index];
    let sumGrades = 0;

    while (badGrades < 2){
        currentInput = input[index]
        index++

        if (currentInput === "Enough" )


        break;

    }


}

exam (["2", "Income", "3", "game info", "6", "Best Player", "4", "Enough"])